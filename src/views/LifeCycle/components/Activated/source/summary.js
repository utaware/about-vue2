export default `
  // callHook(vm, 'activated')
  // -> activateChildComponent
  // -> componentVNodeHooks.insert

  // -> invokeCreateHooks, invokeInsertHook
  // -> createPatchFunction
  // -> patch
  // -> Vue.prototype.__patch__
  // -> Vue.prototype._update
`
